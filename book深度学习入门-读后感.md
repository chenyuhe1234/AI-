#### python基本的数据类型
> 数值型
```java
// 包括整数（int）和浮点数（float）。整数没有小数部分，比如 1、100、-5；浮点数包含小数部分，比如 3.14、0.001。Python 3 中整数没有大小限制，只要内存足够大，你可以处理极大的数字。

C:\Users\Cwx1505274>python
Python 3.12.10 (tags/v3.12.10:0cc8128, Apr  8 2025, 12:21:36) [MSC v.1943 64 bit (AMD64)] on win32
Type "help", "copyright", "credits" or "license" for more information.
>>> a=14
>>> print(a)
14
>>> type(a)
<class 'int'>
>>>
```
***
> 布尔类型
```java
// bool 类型只有两个值：True 和 False。它通常用于条件判断，比如 if 语句中。在底层，True 等同于 1，False 等同于 0，但建议直接使用 True 和 False 以保持代码清晰。
```
> 字符串类型
```java
// str 类型用于表示文本。在 Python 中，字符串是由单引号、双引号或三引号包裹的一串字符，比如 'hello' 或 "world"。字符串是不可变的，意味着一旦创建，就不能修改其中的单个字符，只能创建新的字符串。
```
> 列表类型
```java
// list 是一种有序、可变的集合。它可以包含任意类型的数据，并且支持重复元素。列表使用方括号 [] 表示，比如 [1, 2, 3] 或 ['a', 'b', 'c']。你可以随时向列表中添加、删除或修改元素。
```
> 元组类型
```java
// tuple 与列表类似，也是有序的数据集合，但它是不可变的。一旦创建，就不能修改、添加或删除其中的元素。元组使用圆括号 () 表示，比如 (1, 2, 3)。元组常用于需要保护数据不被修改的场景，比如函数的多个返回值。
```
> 字典类型
```java
// dict 是一种键值对（key-value）的无序集合。每个键必须是唯一的，且通常是不可变类型（如字符串、数字或元组），而值可以是任意类型。字典使用花括号 {} 表示，比如 {'name': 'Alice', 'age': 25}。通过键可以快速地查找对应的值。
```
> 集合类型
```java
// set 是一种无序、不重复的元素集合。它主要用于去重和成员关系测试。集合使用花括号 {} 或 set() 函数创建，比如 {1, 2, 3}。注意，空集合必须用 set() 表示，因为 {} 在 Python 中表示空字典。
```
