#### 1.什么是Context Engineering(上下文工程)
> 是什么
```java
Context Engineering，中文通常称为“上下文工程”或“提示词工程的高级阶段”，简单来说，就是系统性地设计、优化和管理人工智能模型（特别是大语言模型 LLM）在生成回答时所依赖的“背景信息”。
是提示词工程的升级
```
***
- 没有上下文工程时
```java
假设你只问AI：“把这段文字翻译成英文。
如果没有上下文工程，AI可能会懵，因为它不知道那段文字是什么风格，是法律合同还是小红书文案？它只能凭感觉翻，结果可能很不准确。
```
- 有了上下文工程
```java
但如果你用了上下文工程，你会这样提供信息：

角色设定：你是一个资深的外贸专员。
背景信息：我们要给德国客户发一封邮件，拒绝他们的降价要求，但要保持礼貌，强调我们的产品质量和售后服务优势。
待翻译文本：No way. Our price is final. Quality is best.

这时候，AI就会结合“外贸”、“德国客户”、“礼貌拒绝”这些上下文，翻出一句非常得体且专业的英文，而不是干巴巴的“No”。
```
***
#### 上下文工程的核心作用就是提供角色、背景、约束和示例，把模糊的指令变成精准的指令。
***
#### 2.什么是 上下文压缩
> 是什么
```java
上下文压缩（Context Compression） 是指在大语言模型（LLM）处理长文本或大量检索结果时，通过算法或模型技术对输入内容进行精简、提炼或过滤，仅保留与当前任务强相关的核心信息，从而减少 Token 消耗、降低噪声干扰并提升生成质量的技术 
```
> 主要作用
```java
1.降本增效：显著减少输入 Token 数量，降低 API 调用成本及推理延迟。例如，LongLLMLingua 可将输入压缩至约 1/4，同时提升性能  8；HyCo₂ 方法可减少 88.8% 的 Token 消耗 
2.提升准确性：通过过滤无关或冗余信息（噪声），让模型聚焦关键事实，降低幻觉风险，提高长文本推理和知识密集型问答的效果 
3.突破窗口限制：在上下文窗口有限或处理超长文档时，通过压缩保留关键语义，使模型能够处理原本超出容量限制的信息
```
***
#### 3.什么是【压缩时机】
压缩时机是指触发上下文压缩算法的具体条件或时间点，旨在平衡信息保真度与Token消耗。主要包含以下三种触发场景：
```java
1.阈值触发：当上下文窗口中的Token数量或消息条数达到预设上限时自动触发，防止超出模型限制。
2.周期性触发：在长对话或长时间运行的任务中，按固定时间间隔或轮次执行压缩，以维持系统稳定性。
3.输入预处理阶段：在将新输入加入上下文前，先对历史信息进行压缩，确保新信息能高效融入且核心内容不丢失。
```
***




