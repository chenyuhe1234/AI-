#### 问题1:什么是Skill
> 是什么
```java
一个 Skill 是一个封装了特定任务或工作流指令集的简单文件夹。它让 Agent 只需学习一次，就能在后续所有交互中重复使用你的偏好、流程和领域知识。
Skill 是连接“用户意图”和“底层工具”的智能胶水层。它教会Agent不仅“能做什么”，更重要的是“应该如何一步步地、高质量地完成”。
```
***
#### 问题2:一个标准的skill包含以下文件夹的内容:
```java
your-skill-name/
├── SKILL.md # **必需**：核心指令文件，包含 YAML 元数据
├── scripts/ # 可选：可执行脚本 (Python, Bash 等)
├── references/ # 可选：供 Skill.md 按需读取的参考文档
└── assets/ # 可选：用于生成结果的模板、图标等资源
```
***
#### 问题3:skill与单一工具的对比
```java
工具:解决【能做某事】的问题
skill:教会Agent 【如何高质量完成】工作
差异:内嵌了专家级的决策逻辑与路径
```
***
#### 问题4:Skill Prompt Tool 三者有何不同
> Skill(技能)
```java
// 1.核心本质
   // 1.1 一个工作流(Workflow)的知识封装
// 2.形态
   // 2.1 结构化的文件夹 包含指令 脚本 文档
// 3.解决的问题
    // 3.1 应该如何做(How to do it)
// 4.典型场景
    // 4.1 多步骤 可复用的流程 如"根据公司风格生成周报" "执行代码审查并自动修复Sentry告警"
// 5.上下文消耗
    // 5.1 低 通过 "渐进式披露"按需加载 仅在触发时消耗少量的token
```
> Prompt(提示)
```java
// 1.核心本质
  // 1.1 一次性的给予模型的指令(Instruction)
// 2.形态
  // 2.1 自然语言文本
// 3.解决的问题
  // 3.1 这次要做什么(What to do now)
// 4.典型场景
    // 4.1 临时的 一次性的任务 如:"帮我总结这段文字"
// 5.上下文消耗
    // 5.1 高 每次都需要在上下文中完整提供
```
> Tool/Function(工具/函数)
```java
// 1.核心本质
    // 1.1 一个模型可以调用的具体能力(Capability)
// 2.形态
  // 2.1 定义清晰的API接口(如 OpenAI Schema)
// 3.解决的问题
    // 3.1 可以做什么 (What can be done)
// 4.典型场景
    // 4.1 原子化的 确定性的操作 如:查询天气 发送邮件
// 5.上下文消耗
    // 5.1 中等  调用时消耗定义的Token
```
***
#### 问题5:一个健壮的 Skill 应该遵循以下软件工程原则
- 原子化 与单一职责
```java
一个 Skill 应该只做好一件定义明确的事情。避免创建一个“万能”的 Skill，它会变得难以维护和触发。例如，将“项目管理”拆分为“创建冲刺计划”、“生成项目周报”、“同步任务状态”等多个更小的 Skill。
```
- 稳定的输入输出契约
```java
Skill 的触发条件和执行结果应该是可预测的。这不仅仅是技术要求，更是用户信任的基础。你的 SKILL.md 的 description 字段就是这个契约最重要的部分。
```
- 状态管理与 幂等性
```java
如果 Skill 执行的是有副作用的操作（如创建、删除），需要考虑幂等性。多次执行同一个 Skill 不应该导致意外的重复操作。工作流中应包含检查“是否已存在”的逻辑。
```
- 可监控与可观测
```java
Skill 的执行过程应该像任何后端服务一样是可观测的。在 Skill 指令中明确定义关键步骤的日志输出格式，可以帮助你快速定位问题。
```
- 核心要点
```java
把 Skill 当作一个“ 微服务 ”来设计。 它的 SKILL.md 就是 API 文档，scripts/ 目录就是业务逻辑实现，references/ 则是外部依赖文档。
```







