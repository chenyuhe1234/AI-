#### 1.什么是微调（Fine Tuning）
> 是什么
```java
微调（Fine-Tuning）是指在预训练模型（如BERT、GPT等）的基础上，利用特定任务或领域的数据集对模型参数进行进一步调整和训练，使其适应新任务的过程。
```
> 核心原理
```java
微调基于迁移学习思想。预训练模型已在海量通用数据上学习了丰富的语言结构、视觉特征或通用知识。微调则保留这些通用能力，通过更新部分或全部模型参数，让模型学习特定任务的数据分布和规律，从而提升在目标任务上的性能。
```
> 微调的主要分类
- 全参数微调：更新模型的所有参数。效果通常最好，但计算资源消耗大。
- 参数高效微调（PEFT）：只更新模型的一小部分参数（如LoRA、Adapter、Prefix Tuning等），大幅降低资源需求，同时保留预训练模型的通用能力。
***
> 典型场景
```java
// 1.自然语言处理：情感分析、机器翻译、问答系统等
// 2.计算机视觉：特定物体识别、医学图像分析等。
// 3.垂直领域专家：将通用大模型转化为特定行业（如金融、医疗）的专业助手。
```
***
#### 2.什么是RAG 检索增强
> 是什么
```java
检索增强生成（Retrieval-Augmented Generation，简称 RAG）是一种将信息检索与大语言模型（LLM）相结合的技术框架，旨在通过引入外部知识源，弥补大模型在事实准确性、知识时效性及私有数据访问方面的不足
```
> 核心原理
```java
RAG 的核心理念是“先检索，后生成”。它不依赖模型内部预训练的记忆来回答所有问题，而是让模型在回答前先从外部知识库中检索相关信息，基于检索到的证据组织语言
- 检索（Retrieval）：从外部数据源（如文档、数据库）中查找与用户问题最相关的内容。
- 增强（Augmentation）：将检索到的内容作为上下文补充到提示词中。
- 生成（Generation）：大语言模型结合原始问题和检索到的上下文，生成最终回答
```
> 主要优势
```java
- 减少幻觉：通过引用真实的外部数据，显著降低模型编造信息（幻觉）的概率
- 知识时效性：无需重新训练模型，只需更新外部知识库，即可让模型掌握最新信息
- 数据隐私与安全：企业私有数据无需上传至公共模型训练，仅通过检索接口使用，保障数据安全
- 降低成本：相比微调或重新训练大模型，RAG 的实施成本更低，且能灵活应对垂直领域需求 
```
***
> 核心流程
```java
1.索引构建（离线）：将外部文档清洗、分块（Chunking），并通过嵌入模型（Embedding）转化为向量，存储于向量数据库中
2.查询检索（在线）：用户提问后，系统将问题向量化，在向量数据库中检索语义最相似的文本块
3.生成回答：将检索到的文本块与用户问题组合，输入大语言模型，生成基于事实的回答 
```
***
#### 3.什么是语义分片
> 是什么
```java
在大模型领域，语义分片通常指的是将文本数据或知识按照其语义内容（即含义、主题、上下文）进行切分和重组的过程。
大模型中的语义分片是一种智能化的数据预处理或推理策略，它强调按“意思”而非“字数”来划分内容，目的是保持信息的逻辑连贯性，提升模型对长文本、复杂知识或跨模态数据的理解和处理能力。
```
> 语义分片主要有以下几种应用场景和含义：
- 长文本处理与注意力优化
```java
大模型的上下文窗口有限，无法一次性处理超长文档。传统的“固定长度切分”（比如每500字切一段）往往会切断句子或段落之间的逻辑联系，导致语义丢失。而语义分片会根据标点符号、段落结构、主题变化等语义边界来切分文本。例如，在一个完整的论点结束处进行切分，而不是在句子中间强行切断。这样可以保证每个切片内部保持较高的语义完整性，便于后续处理。
```
- 检索增强生成中的知识索引
```java
在使用向量数据库进行RAG（检索增强生成）时，需要将知识库中的文档切成小块以便存储和检索。语义分片在这里意味着使用更智能的算法来划分文本块。比如，利用LLM本身或专门的嵌入模型来判断哪些句子属于同一个主题，然后将它们打包成一个“语义单元”。这样当用户提问时，系统能检索到更完整、更相关的上下文片段，而不是零散的关键词片段，从而提高回答的准确性。
```
- 思维链与推理步骤分解
```java
在复杂推理任务中，语义分片也可以指将一个大问题或长推理过程分解为多个小的、具有明确语义目标的子步骤。例如，先让模型提取事实，再让模型进行逻辑推导，最后得出结论。每个步骤对应一个语义片段，模型可以逐个处理，降低出错概率。
```
- 多模态数据的语义对齐
```java
在多模态大模型中，语义分片还可能涉及将图像、文本等不同模态的数据按照语义内容进行匹配和分割。比如，将图片中的某个区域与描述该区域的文本片段关联起来，确保模型理解的是“图中的苹果”而不是“文字里的苹果”。
```
#### 4.什么是大模型的向量化
> 是什么
```java
大模型的向量化（Vectorization），本质上是将非结构化数据（如文本、图像、音频等）转换为计算机可处理的数值向量的过程。在大模型语境下，它主要指将语言元素映射为高维数值向量，使机器能够理解语义并进行数学运算 
```
> 核心原理
```java
- 数据转换：将人类可读的文字、图片等离散数据，映射到连续的高维向量空间中。每个数据点（如一个词或一句话）被表示为一串数字序列（向量）
- 语义捕捉：通过神经网络（如 Embedding 模型）学习数据的深层特征。在向量空间中，语义相近的数据，其向量距离也更近（例如“苹果”和“橙子”的向量位置接近，“国王”和“君主”同理）
- 数学可计算性：计算机底层仅支持数值运算。向量化后，原本无法直接计算的符号数据具备了线性可加性和距离可度量性，模型可通过矩阵乘法、点积等数学运算来捕捉语义关系和语法结构
```
> 为什么需要向量化
```java
- 机器理解的基础：大模型（LLM）的输入层必须接收数值型数据。向量化是连接人类语言与机器计算的桥梁，没有向量化，模型无法直接处理文本
- 高效检索与匹配：在 RAG（检索增强生成）等应用中，将用户问题和知识库文档都转化为向量后，可以通过计算向量相似度（如余弦相似度、欧氏距离）快速找到最相关的内容，实现精准检索 
- 泛化与推理能力：向量化不仅保留字面信息，还能捕捉抽象模式（如类比关系、情感倾向），提升模型的认知广度和推理深度
```
***
#### 5.什么是大模型的混合检索
> 是什么
```java
混合检索 = 向量检索 + 关键字检索
向量检索（稠密检索）：擅长理解语义。能捕捉“意思相近但用词不同”的内容（如“手机电池耐用”与“延长电池寿命”），但容易在专有名词、特定编号或缩写上产生误判 
关键字检索（稀疏检索，如 BM25）：擅长精确匹配。能精准定位包含特定关键词、代码变量、工单号或行业黑话的文档，但无法理解深层语义
```
> 解决的痛点
```java
- 语义泛化与精确匹配的矛盾：纯向量检索可能因语义相近将“007号工单”误召回为“008号工单”，而混合检索通过关键字检索确保特定标识符的精确命中
- 长尾查询与冷启动问题：对于专业术语或领域外（OOD）查询，关键字检索作为兜底，确保基础相关性，避免大模型“查不准”或产生幻觉
- 提升召回率与准确率：单一检索往往顾此失彼，混合检索通过融合策略，在保持高召回率的同时，显著减少无关内容的干扰 
```
> 为什么需要混合检索？
```java
1.单一检索的局限：
  1.1 纯向量检索：可能遗漏包含关键术语但语义表达不同的文档，或对特定编号敏感度过低
  1.2 纯关键字检索：无法处理同义词、上下文依赖或语义相近但字面不同的查询，导致召回率低下

2.混合检索的效果：
  2.1 在测试中，混合检索相比纯向量或纯关键字检索，能显著提升首条结果的准确率（如提升 28%）和结果多样性
  2.2 它平衡了计算效率与质量，延迟通常介于两者之间（如 150ms vs 50ms/300ms），但准确率大幅提升（如 85% vs 62%/78%）
```
***
#### 6.大模型的应用落地，通常有哪四种常见的技术选型方案？
```java
- 提示工程（Prompt Engineering）
- 检索增强生成（RAG）
- 工作流编排（Workflow）
- 智能体（Agent）
- 模型微调（Fine-tuning / SFT）
```
- 提示工程（Prompt Engineering）
```java
适用场景：轻量级任务，如广告文案生成、新闻摘要、简单的归纳或创造性任务
特点：零训练成本、即时生效、灵活性极高。通过定义模型角色、任务目标和输入输出格式来驱动模型
局限：容易产生幻觉，难以处理多步骤复杂逻辑，且无法保证事实准确性
```
- 检索增强生成（RAG）
```java
适用场景：需要“必须说对事实”且依赖内部资料的场景，如产品政策查询、合规口径回答、企业知识库问答
特点：从外部知识库检索相关信息并注入上下文，使答案有据可查、可追溯，有效解决模型知识滞后和幻觉问题
```
- 工作流编排（Workflow）
```java
1.适用场景：多步骤协作、需要审校、回退或多人参与的场景，如合同审核、报表发布流程
2.特点：将复杂的业务逻辑拆解为明确的节点（Node），通过流程控制实现结构化执行，确保步骤的确定性和可控性 
```
- 智能体（Agent）
```java
1.适用场景：需要自主规划、执行及调用外部工具的场景，如“拉取数据→分析→生成方案→创建工单”的自动化闭环
2.特点：具备自主思考、寻找工具和串联API的能力。通常建议从“受限型 Agent”入手，明确边界与停止条件，以避免不可控行为
```
- 模型微调（Fine-tuning / SFT）
```java
1.适用场景：高频、固定模式的任务，且对特定风格或结构化输出有极高要求，如品牌风格文案生成、特定格式的结构化数据抽取
2.特点：基于已有闭环数据进行监督微调（SFT），相比前几种方案，能更稳定、低成本地处理特定领域的固定任务，但开发成本较高
```
***
#### 7.RAG的整体工作流程涉及哪几个关键步骤？
文档加载与拆分 →→ 向量化与索引存储 →→ 查询向量化 →→ 向量检索 →→ 构建增强提示词 →→ LLM 生成回答
RAG（检索增强生成）的整体工作流程通常分为三个核心阶段：索引阶段（知识库构建）、检索阶段和生成阶段。具体关键步骤如下：
- 索引阶段（Indexing / Knowledge Base Construction）
```java
1.文档加载（Document Loading）：从多种来源（如 PDF、Word、网页、数据库等）加载原始文档
2.文档拆分（Chunking）：将长文档分割成适当大小的文本块（Chunk）。这是关键步骤，需保证语义完整性并适配大模型的上下文窗口（通常每块 200-500 token）
3.向量化（Embedding）：使用嵌入模型（Embedding Model）将文本块转换为高维向量表示（数值向量）
4.存储与索引：将向量存入向量数据库（Vector Database）并建立索引，以支持高效的相似性搜索
```
- 检索阶段（Retrieval）
```java
此阶段在用户发起查询时实时执行，旨在从知识库中找到最相关的信息
1.查询处理与向量化：接收用户原始查询，使用相同的嵌入模型将查询文本转换为向量
2.相似性搜索：在向量数据库中计算查询向量与文档向量之间的相似度（如余弦相似度），检索出最相关的 Top-K 个文档片段 
3.结果重排（可选）：部分高级系统会对检索结果进行重排序（Rerank），以进一步提高相关性 
```
- 生成阶段（Generation）
```java
此阶段结合检索到的上下文与大模型能力，生成最终回答
1.提示词构建（Prompt Engineering）：将用户原始查询与检索到的相关文档片段组合，构建包含上下文的增强提示词（Enhanced Prompt）
2.增强生成：将增强后的提示词输入大型语言模型（LLM），模型基于提供的上下文生成准确、有据可依的回答 
```
#### 8.RAG工作流程中的增强环节主要完成了什么功能？
- 上下文整合
```java
将检索到的文档片段与用户原始问题结合，构建结构化的提示（Prompt），为大语言模型（LLM）提供更丰富的背景信息。例如，通过拼接检索结果和问题，形成类似“根据以下资料回答：[检索内容]，问题：[用户问题]”的输入格式
```
- 事实依据补充
```java
通过引入外部知识库中的真实数据，为LLM生成答案提供可验证的依据，减少模型因依赖静态训练数据而产生的“幻觉”（虚构信息）。例如，在合同解析场景中，增强环节会将合同条款与用户问题结合，确保回答基于实际条款内容 
```
- 提示工程优化
```java
对检索结果进行筛选、排序或格式化处理（如重排序Rerank），确保最相关的信息被优先输入LLM。例如，通过RRF（倒数排名融合）算法对Top-K检索结果重新排序，过滤冗余或低相关性内容
```
- 领域知识适配
```java
针对专业领域（如医疗、法律）或企业私有数据，将领域特定的术语、规则或数据结构注入提示，提升LLM在垂直场景中的准确性。例如，华为的RAG实践通过分层知识库（设计-开发-测试）适配不同产品线需求
```
- 多模态信息融合
```java
在涉及表格、图片等多模态数据时，增强环节会将结构化数据（如表格字段）或视觉信息（如OCR提取的文本）与文本检索结果整合，形成更全面的上下文输入
```
















